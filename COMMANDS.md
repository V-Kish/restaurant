# POS

```bash
pnpm --filter pos exec expo run:ios
pnpm --filter pos exec expo run:android
pnpm --filter pos exec expo start --web
```

# KDS

```bash
pnpm --filter kds exec expo run:ios
pnpm --filter kds exec expo run:android
pnpm --filter kds exec expo start --web
```
